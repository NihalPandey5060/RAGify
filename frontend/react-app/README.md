# RAG Frontend (React)

Minimal Vite + React example showing authentication state and a Logout button.

Run locally:

1. cd frontend/react-app
2. npm install
3. npm run dev

The app expects the backend API at the same origin. If your backend runs on a different host/port, set `API_BASE` constants in the source files.
